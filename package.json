{
  "name": "rfq-visibility-application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only visibility layer over the Procurement Tracker.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
