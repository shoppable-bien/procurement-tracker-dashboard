# Proof ledger

Proofs: not required for the initial implementation.

The risk is bounded by using a fixture-backed source adapter, explicit normalization tests, and a replaceable snapshot boundary. Revisit this if authenticated Google Sheets access or a concurrent deployment is introduced.
